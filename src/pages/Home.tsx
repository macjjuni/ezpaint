import { Canvas } from '@/components'

const Home = () => {

  return (
    <section className="flex justify-center items-center w-full h-full">
      <Canvas />
    </section>
  )
}

export default Home
